package practice_one;

public class day20 {

	public static void main(String[] args) {
		
		int[]arr=new int[n];         //   n.length-----------> o(1) because its a built in variable not method
//		int [][]arr=new int[n][m];      n.length     arr[i].length
//		int [][][]arr=new int[n][m][o];    n.length    arr[i].length     arr[i][j].length
		System.out.println(arr.getClass());   //this will print class [I 
		//for 2 dimension it prints class [[I
		//for class it prints 
		//class day20 I mean if its 
		//day20 d=new day20(); later,,, sysout(d.getClass());
	}

}
